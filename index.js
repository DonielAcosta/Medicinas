/**
 * @format
 */

import {AppRegistry} from 'react-native';
import { MedicinasApp } from './src/MedicinasApp';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => MedicinasApp);
